import { CurrentDateValidatorDirective } from './current-date-validator.directive';

describe('CurrentDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
