import { UniqueMailClientValidatorDirective } from './unique-mail-client-validator.directive';

describe('UniqueMailClientValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueMailClientValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
