import { UniqueCityValidatorDirective } from './unique-city-validator.directive';

describe('UniqueCityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
