import { UniqueCodePostalValidatorDirective } from './unique-code-postal-validator.directive';

describe('UniqueCodePostalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCodePostalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
