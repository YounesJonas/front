import { UniqueMailValidatorDirective } from './unique-mail-validator.directive';

describe('UniqueMailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueMailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
