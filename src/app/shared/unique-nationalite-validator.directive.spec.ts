import { UniqueNationaliteValidatorDirective } from './unique-nationalite-validator.directive';

describe('UniqueNationaliteValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueNationaliteValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
