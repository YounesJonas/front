import { TwoHoursValidatorDirective } from './two-hours-validator.directive';

describe('TwoHoursValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoHoursValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
