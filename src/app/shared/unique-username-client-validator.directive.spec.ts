import { UniqueUsernameClientValidatorDirective } from './unique-username-client-validator.directive';

describe('UniqueUsernameClientValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUsernameClientValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
