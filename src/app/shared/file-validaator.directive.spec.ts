import { FileValidaatorDirective } from './file-validaator.directive';

describe('FileValidaatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileValidaatorDirective();
    expect(directive).toBeTruthy();
  });
});
